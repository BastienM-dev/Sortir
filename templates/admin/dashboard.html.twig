{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
    <h1 class="text-center">Page d'administration</h1>

    <div>
        <ul>
            <li><a href="{{ path('admin_admin_users') }}">Gérer les utilisateurs</a></li>
            <li><a href="{{ path('admin_campus') }}">Gérer les campus</a></li>
            <li><a href="{{ path('admin_villes') }}">Gérer les villes</a></li>
        </ul>
    </div>

    {% if is_granted('ROLE_ADMIN') %}
        Sorties | {{ parent() }}
    {% endif %}
{% endblock %}