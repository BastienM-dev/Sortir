{% extends 'base.html.twig' %}

{% block title %}
    Sorties | {{ parent() }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block body %}
    <div class="admin-page">
        <h1 class="admin-title">Page d'administration</h1>

    <div>
        <a class="btn btn-primary" href="{{ path('site_list') }}">Gérer les campus</a>
        <a class="btn btn-primary" href="{{ path('ville_list') }}">Gérer les villes</a>
        <a class="btn btn-primary" href="{{ path('admin_users') }}">Gérer les utilisateurs</a>
    </div>
{% endblock %}