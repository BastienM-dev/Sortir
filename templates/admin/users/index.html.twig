{% extends 'base.html.twig' %}
{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>

    {# Affichage des messages flash (succès / warning / erreur) #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('warning') %}
        <div class="alert alert-warning">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}

    {# Formulaire de suppression multiple (bulk delete) #}
    <form method="post" action="{{ path('admin_users_delete') }}">
        {# 🔒 Token CSRF associé à l'action "bulk_delete_users" #}
        <input type="hidden" name="_token" value="{{ csrf_token('bulk_delete_users') }}">

        <table border="1" cellpadding="8" cellspacing="0">
            <thead>
            <tr>
                {# Checkbox “tout sélectionner” (optionnel) #}
                <th style="text-align:center;">
                    <input
                            type="checkbox"
                            id="check_all"
                            aria-label="Tout sélectionner"
                            onclick="
                        const checks = document.querySelectorAll('input[name=&quot;user_ids[]&quot;]');
                        checks.forEach(c => c.checked = this.checked);
                        document.getElementById('btn_delete').disabled = ![...checks].some(c => c.checked);
                    "
                    >
                </th>
                <th>Pseudo</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th>Actif</th>
                <th>Admin</th>
            </tr>
            </thead>

            <tbody>
            {# Parcours de la liste des utilisateurs fournie par le contrôleur #}
            {% for u in users %}
                <tr>
                    <td style="text-align:center;">
                        {# Chaque checkbox envoie un tableau user_ids[] au POST #}
                        <input
                                type="checkbox"
                                name="user_ids[]"
                                value="{{ u.id }}"
                                aria-label="Sélectionner {{ u.pseudo }}"
                                onchange="
                            const checks = document.querySelectorAll('input[name=&quot;user_ids[]&quot;]');
                            document.getElementById('btn_delete').disabled = ![...checks].some(c => c.checked);
                            document.getElementById('check_all').checked = [...checks].every(c => c.checked);
                        "
                        >
                    </td>

                    {# Infos utilisateur #}
                    <td>{{ u.pseudo }}</td>
                    <td>{{ u.nom }}</td>
                    <td>{{ u.prenom }}</td>
                    <td>{{ u.mail }}</td>
                    <td>{{ u.actif ? 'Oui' : 'Non' }}</td>
                    <td>{{ u.administrateur ? 'Oui' : 'Non' }}</td>
                </tr>

                {# Cas où la liste est vide #}
            {% else %}
                <tr>
                    <td colspan="7">Aucun utilisateur</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div style="margin-top: 16px;">
            {# Bouton désactivé par défaut si rien n’est coché (optionnel) #}
            <button
                    id="btn_delete"
                    type="submit"
                    disabled
                    onclick="return confirm('Supprimer les utilisateurs sélectionnés ?')"
            >
                Supprimer la sélection
            </button>
        </div>
    </form>
{% endblock %}
