{% extends 'base.html.twig' %}

{% block title %}Créer un lieu{% endblock %}

{% block body %}
    <h1>Créer un nouveau lieu</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Créer le lieu</button>
    <a href="{{ path('admin_lieu_list') }}">Annuler</a>
    {{ form_end(form) }}
{% endblock %}